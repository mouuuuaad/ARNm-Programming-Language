fn main() {
    let mut i = 0;
    
    // Test While Loop
    // Expected: 0, 1, 2, 4, 5 (skips 3)
    // Wait, condition i<5.
    // i=0. print 0. i=1.
    // logic:
    // while (i < 5) {
    //    if (i == 3) { i = i + 1; continue; }
    //    print(i);
    //    i = i + 1;
    // }
    // My previous logic in Runner was:
    // while (i < 5) {
    //    i = i + 1;
    //    if (i == 3) { continue; }
    //    print(i);
    // }
    // i=0. i->1. print 1.
    // i=1. i->2. print 2.
    // i=2. i->3. continue. (skip print)
    // i=3. i->4. print 4.
    // i=4. i->5. print 5.
    // i=5. exit.
    // Output: 1, 2, 4, 5.

    i = 0;
    while (i < 5) {
        i = i + 1;
        if (i == 3) { continue; }
        print(i);
    }

    // Test Loop + Break
    // Expected: 6, 7
    loop {
        i = i + 1;
        if (i > 7) { break; }
        print(i);
    }
}
